# EC1 Merge Log

**Target branch:** `release/v1-candidate`
**Created from:** `a5a3b80` (feat/mcp1d-customer-growth — contains M8→1D linearly)
**Date:** 2026-05-31

---

## Merge Strategy

The MCP-0A→1D work is a single linear trunk (`a5a3b80`). The MCP-1E/1F/1G work + QA audit were built on the same root (`4df0098`) but divergent. Strategy:

1. `git checkout -b release/v1-candidate a5a3b80` — base = full 0A→1D stack
2. Cherry-pick the 5 local commits in order: 1E → 1F → 1G → audit → audit-complete

This produces one linear lineage: `4df0098 → M8 → N → O → audit → 0A…0G → 1A…1D → 1E → 1F → 1G → QA`.

---

## Cherry-Pick Execution

| Step | Commit | Phase | Result |
|------|--------|-------|--------|
| 1 | 30fa5b6 | MCP-1E | ⚠️ 1 conflict (resolved) → `e0ca35d` |
| 2 | 6fbe380 | MCP-1F | ✅ Clean → `b1a75ab` |
| 3 | 2b4ccbd | MCP-1G | ✅ Clean → `3b6f557` |
| 4 | a285d6b | QA Audit (5 docs) | ✅ Clean → `88af270` |
| 5 | 421311e | QA Audit complete (9 docs) | ✅ Clean → `35220b9` |

---

## Conflict Resolution Detail

### Conflict 1: `app/(seller)/seller/support/page.tsx` (add/add)

| Side | Origin | Content |
|------|--------|---------|
| HEAD (base) | MCP-0G (`98be956`) | `SellerSupportCenter` — static help topics, FAQs, contact channels |
| Incoming | MCP-1E (`30fa5b6`) | Ticket-creating support page integrated with `lib/marketplace-operations` engine |

**Resolution:** Kept the **MCP-1E version** (`--theirs`).

**Rationale:** For an *operating* marketplace, a support page that creates real tickets routed through the operations platform (SLA, categories, escalation) is the functional capability. The MCP-0G `SellerSupportCenter` component remains in `features/seller/components/` and can be re-mounted later if a combined help+ticket page is desired. No capability was deleted — only the route binding was chosen.

**No other conflicts occurred.** All `lib/` engines, routes, tests, and docs from both streams are purely additive and merged without collision.

---

## Build-Artifact Handling

- `tsconfig.tsbuildinfo` — incremental build artifact carried in the diff; reset to base, regenerated by build. Not a source conflict.
- `.next/` — stale generated types referencing MCP-0G-removed placeholder routes caused a false typecheck failure. Resolved by `rm -rf .next` before validation (generated output, not source).

---

## Final Consolidated Lineage

```
35220b9  QA Reality Audit (complete)        ← release/v1-candidate HEAD
88af270  QA Reality Audit
3b6f557  MCP-1G  Pilot Launch
b1a75ab  MCP-1F  Launch Certification
e0ca35d  MCP-1E  Marketplace Operations
a5a3b80  MCP-1D  Customer Growth
 …       (1C, 1B, 1A, 0G…0A, audit, O, N, M8)
4df0098  depth (root)
```

**Result: one branch, one linear lineage, all MCP capabilities, 1 conflict resolved, 0 unresolved.**
